Messenger — учебное веб-приложение

Запуск:

1. Установить Python.
2. Открыть командную строку в этой папке.
3. Выполнить:

   python -m pip install -r requirements.txt

4. Запустить:

   python app.py

5. Открыть:

   http://127.0.0.1:8000

Для доступа с телефона в локальной сети:
   http://IP_КОМПЬЮТЕРА:8000

Первый администратор при новой базе:
   логин:  admin
   пароль: admin12345

Если у вас уже есть messenger.db от предыдущей версии:
- можно скопировать старый файл data\messenger.db в новую папку data\
- пользователи и сообщения сохранятся;
- новая таблица attachments создастся автоматически.

Изображения:
- JPG
- PNG
- WebP
- GIF
- до 20 МБ

Изображения сохраняются в:
   data\uploads\

Важно:
для публикации в Интернет используйте HTTPS через reverse proxy.


Очистка переписки:
- кнопка "Очистить переписку" скрывает всю текущую историю только у нажавшего пользователя;
- у второго участника история остаётся;
- администратор продолжает видеть всю переписку;
- сообщения и изображения физически из базы/архива не удаляются;
- новые сообщения после очистки снова отображаются;
- существующая messenger.db обновится автоматически (добавится таблица chat_clears).


Ответы на сообщения:
- возле сообщения есть кнопка ↩ "Ответить";
- выбранное сообщение показывается над полем ввода;
- можно отвечать и на текст, и на изображение;
- если исходное сообщение было скрыто пользователем через "Очистить переписку",
  цитата этого старого сообщения у него больше не показывается;
- сам новый ответ при этом остаётся виден как обычное сообщение;
- у второго пользователя цитата остаётся, если он свою переписку не очищал;
- администратор видит полную историю и все связи ответов;
- существующая messenger.db обновляется автоматически: добавляется поле reply_to_message_id.


ВАЖНО ДЛЯ ЭТОЙ СБОРКИ
=====================
Это исправленная сборка: 2026.08.09-replies-v2.

После запуска в консоли должно быть:
    Messenger version: 2026.08.09-replies-v2
    Функция ответов на сообщения: ВКЛЮЧЕНА

В верхней панели чата должно быть видно "Messenger v4".

На ПК возле КАЖДОГО сообщения постоянно видна кнопка:
    ↩ Ответить

На телефоне возле каждого сообщения видна круглая кнопка ↩.

Если вы не видите "v4", значит запущена старая папка/старый процесс.
Перед запуском новой версии остановите старый сервер Ctrl+C.

Существующую базу data\messenger.db и папку data\uploads
можно перенести из предыдущей версии.


Исправления replies-v3:
- кнопка ответа снова маленькая и ненавязчивая;
- на ПК она показывается при наведении на конкретное сообщение;
- на телефоне она небольшая и полупрозрачная;
- кнопка находится внутри правого верхнего угла сообщения, а не сбоку;
- цитируемое сообщение визуально отделено отдельным блоком;
- имя автора цитаты и текст цитаты имеют отдельное оформление;
- основной текст ответа идёт ниже цитаты и не сливается с ней.


Исправления replies-v4:
- кнопка ответа вынесена за пределы пузыря сообщения;
- сама карточка сообщения больше не увеличивается из-за кнопки;
- кнопка уменьшена до 20-22 px, прозрачная и нейтральная;
- у своих сообщений кнопка находится слева от пузыря;
- у входящих сообщений кнопка находится справа от пузыря;
- оформление цитаты продублировано встроенными стилями прямо в chat.html,
  поэтому оно не зависит от старой версии style.css в кеше браузера;
- цитата имеет отдельный фон, левую вертикальную линию, имя автора и
  уменьшенный текст; основной ответ идёт отдельной строкой ниже.


Исправления replies-v6:
- снова используется стабильная вёрстка v4;
- поле ввода на ПК нормальной высоты 44 px и занимает всё доступное место;
- мобильная вёрстка не переводилась на новую сетку;
- textarea растёт только при вводе нескольких строк;
- ПК: Enter отправляет сообщение;
- ПК: Shift+Enter переносит строку;
- телефон/планшет: Enter переносит строку;
- телефон/планшет: отправка только кнопкой ➤;
- стрелки входящих сообщений сохранены как в удачной версии;
- строка своего сообщения теперь имеет ширину по содержимому и прижата вправо,
  поэтому стрелка остаётся непосредственно рядом со своим пузырём.


Брендинг Манул Чат:
- static\image\logo_max.png — крупный логотип на странице авторизации;
- static\image\logo_min.png — favicon сайта и иконка интерфейса;
- logo_min.png показывается в левом нижнем углу интерфейса чата;
- исходные изображения пользователя встроены без перерисовки.


Изменение logo-v2:
- logo_min.png больше не показывается в левом нижнем углу чата;
- иконка перенесена в верхнюю панель непосредственно перед надписью "Манул Чат v8";
- на телефоне размер иконки 32x32 px;
- на ПК размер иконки 36x36 px;
- favicon и крупный logo_max.png на странице авторизации сохранены без изменений.


============================================================
PRODUCTION STAGE 1
============================================================

Эта сборка не меняет логику чата и интерфейс.

Что добавлено:
- случайный пароль первого администратора;
- development / production режимы;
- Secure cookie в production;
- кеширование статики в production;
- versioned URL для CSS и логотипов;
- SQLite WAL;
- busy_timeout 10 секунд;
- foreign_keys;
- удаление просроченных сессий.

НОВАЯ БАЗА
При первом запуске новой базы программа создаст admin
и покажет случайный пароль в консоли.

СУЩЕСТВУЮЩАЯ БАЗА
Если в messenger.db уже есть admin, его пароль НЕ меняется.

DEVELOPMENT
Windows:
    run_development.bat

или:
    set APP_ENV=development
    python app.py

В этом режиме можно работать по обычному HTTP.

PRODUCTION
Windows:
    run_production.bat

или:
    set APP_ENV=production
    python app.py

В production session cookie имеет флаг Secure.
Поэтому использовать нужно HTTPS через reverse proxy.

Кеширование в production:
- HTML: no-cache / перепроверка у сервера;
- API и защищённые вложения: no-store;
- CSS и другая статика: 7 дней;
- логотипы/favicon: 30 дней.

Рекомендуемая схема:
    Интернет
       |
      HTTPS :443
       |
    Reverse Proxy
       |
    http://127.0.0.1:8000
       |
    Манул Чат

Порт 8000 напрямую в Интернет не публиковать.

Следующий этап:
- CSRF;
- WebSocket Origin check;
- закрытие активного WebSocket при блокировке пользователя.
